# mk-forge

Генератор расчетных моделей маркетинговых кампаний (МК) топливной программы в формате Excel.

## Зачем

Акции считали руками в Excel. Результаты были вбиты в ячейки, границы диапазонов
вписаны вручную и разъехались между колонками, а допущения жили в ячейках и
восстанавливались только чтением формул. К такой книге нельзя задать вопрос:
непонятно, откуда взялось число и что будет, если поменять условие.

mk-forge собирает книгу, в которой все расчеты остаются формулами Excel — она
приходит экономисту живой. Параллельно то же самое считает ядро на Python: оно
не заполняет ячейки, а служит вторым независимым путем, с которым книга
сверяется перед отправкой. Расходятся — книгу отдавать нельзя.

## Два режима

**Своя книга.** Из выгрузки транзакций и договоров, шкалы СТП из уведомления и
конфига акции собирается книга с формулами, пересчитывается и сверяется с ядром.

**Чужая книга.** Экономист присылает свой файл и просит исправить расчет. Тогда
правится его файл: форма отчета не меняется, в книгу не добавляется ничего —
ни листа, ни колонки, ни примечания. Порядок работы и три обязательные сверки
лежат в скилле `.claude/skills/patch-workbook/`.

## Страница «Акция»

Страница в браузере: поля вместо yaml, числа меняются во время набора,
три графика, по кнопке — книга с вердиктами и черновиком абзаца для пересылки.
Готовые книги скачиваются с нее же. Страница считает ядром, поэтому LibreOffice
ей не нужен; он нужен только сборке.

Страница — рабочая поверхность, Excel остается на вывоз. Параметры она пишет
в конфиг, а книгу собирает уже из файла: источник истины один. Страница никуда
не обращается — ни за шрифтами, ни за библиотеками графиков, потому что на ней
числа по реальному пулу. Открывается она только на своей машине: с другой машины
в сети ее не видно. Без данных страница открывается с приглашением загрузить выгрузку.

Данные загружаются на ней же: выгрузку `.xlsx` и уведомление о СТП `.docx` перетаскивают
в блок «Данные», обработка идет с шагами, таблицу отделений заполняют формой — отделения
из выгрузки, регионы из ее прогноза маржи. Если обработка не удалась, прежние данные
остаются на месте. Сырые файлы после обработки удаляются, удачной или нет.

## Установка на Mac

После установки на этом Mac работает страница: в нее перетаскивают выгрузку
и уведомление о СТП, задают параметры акции и скачивают готовую книгу. Файлы
остаются на этом компьютере, в сеть ничего не уходит.

Нужно один раз: поставить Docker Desktop и выполнить три команды. Дальше
страница всегда под рукой, в том числе после перезагрузки.

### 1. Docker Desktop

1. Скачать с https://docs.docker.com/desktop/setup/install/mac-install/
   и установить: перетащить в «Программы». На странице два варианта, Apple
   silicon и Intel; какой чип, видно слева вверху: яблоко → «Об этом Mac».
2. Запустить Docker Desktop. При первом запуске он попросит принять
   соглашение и, возможно, пароль от Mac.
3. Дождаться, когда в строке меню появится значок кита и перестанет двигаться.

### 2. Три команды

Открыть Терминал: ⌘ Пробел, набрать «Терминал», Enter. Вставлять по одной
команде, после каждой Enter, и дождаться приглашения для следующей.

```bash
mkdir -p ~/mk-forge && cd ~/mk-forge
```

```bash
curl -fsSLo compose.yaml https://raw.githubusercontent.com/blashkin/mk-forge/main/compose.yaml
```

```bash
docker compose up -d
```

Третья команда в первый раз скачивает около 250 МБ. Когда она закончится,
страница готова.

### 3. Страница

Открыть в браузере http://127.0.0.1:8765. Версия видна в шапке. Дальше
страница ведет сама, порядок такой:

1. В блок «Данные» перетащить выгрузку `.xlsx` и уведомление о СТП `.docx`,
   можно вместе. Сам файл не меняется, страница берет копию. Она покажет шаги
   обработки и итог: сколько договоров, транзакций и отделений загружено.
2. В «Таблице отделений» для каждого отделения выбрать регион прогноза маржи
   и нажать «Сохранить таблицу отделений». Пары по названию не подбираются:
   если не уверены, уточнить до сохранения. После сохранения появится форма
   акции.
3. Проверить параметры в форме и нажать «Собрать книгу». Дождаться строки
   «книга собрана и сошлась с расчетом».
4. В «Готовых книгах» нажать «скачать книгу».

Закрыть вкладку можно в любой момент: данные, параметры и книги остаются.
После перезагрузки Mac ничего вводить не нужно: как только запустится Docker
Desktop, страница снова на месте.

### 4. Обновление

В Терминале:

```bash
cd ~/mk-forge && docker compose pull && docker compose up -d
```

Данные и книги остаются. Новая версия видна в шапке страницы.

### Если страница не открывается

1. Docker Desktop запущен? Если нет, запустить и подождать минуту.
2. Повторить в Терминале: `cd ~/mk-forge && docker compose up -d`.
3. Что пишет контейнер, видно в Docker Desktop: Containers → mk-forge → Logs.

## Как устроен контейнер

Образ несет Python, LibreOffice и страницу; все состояние — данные, ключ
обезличивания, конфиги, книги — лежит в томе `mk-forge`. В том руками не ходят:
выгрузка попадает туда загрузкой со страницы, книга уходит скачиванием.
Остановить — `docker compose down`, все остается в томе.

`compose.yaml` берет образ из ghcr, под amd64 и arm64. Версия задается переменной
`MK_FORGE_TAG`, без нее — `latest`; какая запущена, видно на странице.

```bash
MK_FORGE_TAG=0.1.0 docker compose up -d         # откатиться на 0.1.0
```

Выпуск — тег `vX.Y.Z`: CI собирает образ под обе архитектуры, проверяет `mk-forge doctor`
и отсутствие данных в образе и публикует его с метками `X.Y.Z`, `X.Y` и `latest`
(`.github/workflows/image.yml`). Изменения сборки в `main` проверяются так же, но
не публикуются. Собирать образ на месте под именем из `compose.yaml` не надо:
локальный образ заслонит опубликованный.

В пустой том при первом старте кладутся образцы конфигов, дальше их правит страница.
Какая акция открыта, задает переменная `MK_FORGE_CONFIG` в `compose.yaml`. Порт
публикуется только на `127.0.0.1` хоста, и это единственное, что закрывает страницу
от сети. Контейнер перезапускается сам, в том числе после перезагрузки машины.

Проверить на маке, что получат другие, — тот же образ из рабочего дерева, корнем
данных служит папка проекта:

```bash
MK_FORGE_CONFIG=configs/<акция>.yaml docker compose -f compose.dev.yaml up --build
```

Параметры пишутся в конфиг проекта, книги ложатся в его `out/`, ключ берется из его
`.env`. Загрузка выгрузки на такой странице переписывает `data/anon` и таблицу
соответствия проекта — как и на странице из исходников. Перезапуска у этого контейнера
нет, чтобы он не держал порт, пока страница запущена из исходников.

## Как завести из исходников

```bash
uv sync
uv run mk-forge doctor                                     # что найдено, чего не хватает
uv run mk-forge prepare "data/raw/<выгрузка>.xlsx" \
    --notice "data/raw/<уведомление>.docx"                 # обезличить и вынуть параметры
uv run mk-forge build configs/<акция>.yaml                 # собрать книгу с формулами
uv run mk-forge validate "out/рабочие/<книга>.xlsx" \
    --config configs/<акция>.yaml                          # пересчитать и сверить с ядром
uv run mk-forge restore "out/рабочие/<книга>.xlsx"         # вернуть настоящие номера
uv run mk-forge page configs/<акция>.yaml                  # страница на http://127.0.0.1:8765
uv run mk-forge guide                                      # гайд «Установка на Mac» одним html в out/, для пересылки
```

Тот же гайд опубликован на GitHub Pages: https://blashkin.github.io/mk-forge/. Его
пересобирает `.github/workflows/pages.yml` при каждом изменении README в `main`,
так что вместо файла можно переслать ссылку.

Пути считаются от корня данных, а не от текущей папки. Корень — переменная
`MK_FORGE_HOME` с абсолютным путем, без нее — папка проекта; `doctor` печатает,
какой найден. Из другой папки команды запускаются через
`uv run --project <папка проекта> mk-forge …`. Ключ обезличивания берется
из `MK_FORGE_HMAC_KEY` или из `.env` в корне. Если таблица соответствия есть,
а ключа нет, `prepare` отказывает и нового ключа не создает: с новым ключом
псевдонимы не совпали бы со старыми.

Кроме выгрузки нужна таблица отделений `data/anon/branch_regions.csv`: `prepare` ее
не создает, ее ведут руками или заполняют формой на странице (см. `data/README.md`).

Сборка и пересчет идут в `out/рабочие/`, готовая книга ложится в `out/`. Порядок
важен: `validate` работает по псевдонимам, поэтому настоящие номера договоров
возвращаются последним шагом.

Для пересчета нужен LibreOffice: `brew install --cask libreoffice` на macOS,
`apt install libreoffice-calc` на Linux.

## Где остановились

Основной цикл замкнут: сырая выгрузка превращается в проверенную книгу. Числа
сходятся тремя независимыми путями — книга, собранная руками; ядро на Python;
формулы книги, которую пишет генератор.

Механика пока одна: надбавка к СТП. Глубина задается либо одним уровнем на весь
пул, либо распределением по долям пула, когда скидку раздают менеджеры по одному.
Новые участники считаются как план минус текущий пул, а если плана нет — по
фактическому темпу подключения из дат договоров.

Расчет перестал быть командной строкой: страница «Акция» показывает числа сразу,
а книгу собирает по кнопке. Слой «задание» отделен от транспорта, так что вторым
фасадом может встать что угодно — расчет от этого не удвоится.

Страница живет и в контейнере по образцу Open WebUI: одна команда запуска, все
состояние в одном томе, выгрузка загружается на страницу, книга скачивается с нее же.
Дальше — работать без программиста, и не только здесь: образ в ghcr для пользователя,
доверие к данным, журнал посчитанного. Список работ с причинами — в `PLAN.md`.

Отдельного расчета по трассовым АЗС нет и не будет, пока в выгрузке нет признака
трассовой АЗС.

Решения, принятые по ходу, и чему научили свои дефекты — в `PLAN.md`.

## Где что лежит

```
mkforge/     код: ядро расчета, механики акций, сборка книги, задание, страница
configs/     условные примеры; настоящие конфиги лежат рядом и в git не попадают
data/raw/    сырые выгрузки, с машины не уходят
data/anon/   обезличенные данные, только с ними работают сессии и CI
out/         готовые книги; правка/ — правки чужих книг, рабочие/ — копии на псевдонимах
reference/   эталон первой сборки, для сверки логики, только локально
tests/       синтетический пул строится на лету, реальных данных нет
.claude/     правила проекта и скиллы
```

## Правила и данные

- `CLAUDE.md` — как устроена работа и что считается поставкой.
- `PLAN.md` — решения, принятые по ходу, и чему научили свои дефекты.
- `data/README.md` — состав выгрузки и обезличивание.

Сырые выгрузки с машины не уходят. В git не попадают ни номера договоров, ни
выручка, ни данные клиентов — ни в каком виде, включая фикстуры: тестовый пул
синтетический и строится на лету. Репозиторий публичный, поэтому в нем нет и
настоящих конфигов, отделений, шкал и чисел эталона: конфиги в `configs/` и числа
в тестах условные.
