"""Слой «задание»: на входе конфиг акции, на выходе числа и книга.

Про транспорт здесь не знает никто. Страница в браузере — первый фасад над этим
слоем, и если когда-нибудь появится второй (бот, почта), он возьмет те же
функции, а не заведет второй расчет.

Отсюда запрет на импорт `http`, `socketserver`, `urllib` и даже `json`: словарь
собирает задание, сериализует его транспорт. Запрет держится тестом
`tests/test_page_boundary.py`, а не договоренностью.
"""
